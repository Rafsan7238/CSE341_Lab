DATA SEGMENT
ENDS

CODE SEGMENT
	START:

	MOV AX, DATA
	MOV DS, AX
	MOV ES, AX

	MOV CX, 5
	MOV AH, 1

	INPUT:
	INT 21H
	LOOP INPUT

	MOV AH, 2
	MOV DL, 0DH
	INT 21H

	MOV CX, 5
	MOV DL, 'X'

	PRINT:
	INT 21H
	LOOP PRINT

	MOV AX, 4C00H
	INT 21H

ENDS
END START